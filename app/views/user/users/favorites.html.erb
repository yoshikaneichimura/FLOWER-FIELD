<div class="container">
  <% if @user == current_user %>
    <h5 class="text-center mt-3">あなたのお気に入りの投稿</h5>
  <% else %>
    <h5 class="text-center mt-3">このユーザーお気に入りに追加した投稿</h5>
  <% end %>
  <% if @favorites.present? %>
    <%= render 'user/post_images/flowers', post_images: @favorites %>
  <% else %>
    <h3 class="text-center mt-5">お気に入りに追加された投稿はありません</h3>
  <% end %>
</div>