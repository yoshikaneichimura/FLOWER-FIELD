<div class="container">
  <% if @user == current_user %>
    <h3 class="text-center mt-3">あなたのお気に入りの投稿</h3>
  <% else %>
    <h5 class="text-center mt-3">このユーザーお気に入りに追加した投稿</h5>
  <% end %>
  <% if @favorites.present? %>
    <%= render 'user/post_images/flowers', post_images: @favorites %>
  <% else %>
    <div class="infometion">
      <%= image_tag("green-flower.png")%>
      <h5>＊ お気に入りの投稿は投稿まだはありません。</h5>
    </div>
  <% end %>
</div>