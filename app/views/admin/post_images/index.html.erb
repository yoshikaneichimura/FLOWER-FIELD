<div class="container">
  <h3 class="text-center mt-3">投稿一覧</h3>
  <% if @post_images.exists? %>
    <%= form_with url: admin_post_images_path, method: :get, class: 'boards_searchForm' do %>
    <form>
      <h6 class="mt-4 ml-5">対象の地域を選択して下さい</h6>
      <div class="form-group col-xs-10 col-sm-5 m-3">
        <%= select_tag :tag_id,
        options_from_collection_for_select(Tag.all, :id, :area, params[:tag_id]),
        {
          prompt: 'エリアで絞り込み',
          class: 'form-control',
          onchange: 'submit(this.form);'
        }
        %>
      </div>
    </form>
    <% end %>
    <div class="my-3">
      <%= render 'user/post_images/flowers' ,post_images: @post_images %>
    <% else %>
      <h1 class="text-center mt-5">投稿はまだありません</h1>
    <% end %>
  </div>
</div>
